<div id='root'>
</div>
<script type="text/javascript" id="bootstrap-current-user">
  <% if user_signed_in? %>
    localStorage.setItem(
      'currentUser',
      JSON.stringify(
        <%= (render 'api/users/user.json.jbuilder', user: current_user).html_safe %>
      )
    )
  <% end %>
</script>
